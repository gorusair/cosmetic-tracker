{
  "name": "cosmetic-app",
  "version": "1.0.0",
  "description": "Skinin app using Firebase Hosting",
  "main": "index.js",
  "scripts": {
    "start": "firebase emulators:start --only hosting",
    "copy-html": "mkdir -p public && cp -f index.html public/",
    "copy-assets": "mkdir -p public && cp -Rf assets public/ || echo 'assets 폴더 없음 건너뜀'",
    "build": "npm run copy-html && npm run copy-assets && echo '✅ 빌드 완료 - public 폴더 업데이트됨'",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "author": "",
  "license": "MIT"
}
